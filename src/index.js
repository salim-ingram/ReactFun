import React from "react"
import ReactDOM from "react-dom"
import CollectionContainer from "./components/CollectionContainer"

ReactDOM.render(<CollectionContainer />, document.getElementById("root"))